git remote add upstream [url] 
is used to add the upstream repo as a remote. You can then keep your forked repo synced with the upstream version while making changes, using 'fetch' and 'merge'.
Upstream - information flows from the upstream to the downstream.